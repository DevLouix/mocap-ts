/**
 * Public motion-production workspace contracts.
 *
 * This entrypoint is intentionally data-only. Persistence, API handlers,
 * queue providers, renderers, and UI editors can depend on these contracts
 * without importing one another.
 */
export * from './types.js';
