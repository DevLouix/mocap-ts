{
  "name": "mocap-ts",
  "version": "0.1.0",
  "description": "Video to BVH motion capture \u2014 pure TypeScript, commercially licensed",
  "type": "module",
  "main": "dist/pipeline.js",
  "bin": {
    "mocap-ts": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node dist/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  },
  "dependencies": {
    "@tensorflow/tfjs-node": "^4.22.0",
    "@tensorflow-models/pose-detection": "^2.1.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@tensorflow/tfjs-node",
      "esbuild"
    ]
  }
}
