# Where mocap data (jobs, uploads, output BVH) lives on disk.
# Defaults to <repo>/.mocap — override in containers / deployments.
MOCAP_DATA_DIR=

# Set to "1" to print per-stage progress + ffmpeg/yt-dlp output in the worker.
MOCAP_VERBOSE=0

# Port the Next.js dev server binds to.
PORT=3000
