{
  "name": "@mocap-ts/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Durable CPU motion-processing worker for mocap-ts.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@mocap-ts/core": "workspace:*",
    "@mocap-ts/db": "workspace:*",
    "@mocap-ts/queue": "workspace:*",
    "@mocap-ts/storage": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
